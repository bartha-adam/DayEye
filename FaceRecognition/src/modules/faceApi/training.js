let training = function (apiClient) {
  let module = this;
  module.apiClient = apiClient;

  function startTraining(groupName) {
    
  }

  return {
    startTraining
  }
};

module.exports = training;
